#ifndef LIBCPP_NEW
#define LIBCPP_NEW

extern "C"
{
	#include "libthoth/type.h"
}

// New operators
void* operator new(size_t size);
void* operator new[](size_t size);

// Delete operators
void operator delete(void* ptr);
void operator delete[](void* ptr);

#endif
